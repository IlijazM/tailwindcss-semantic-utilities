# @generated by Gemini

name: Test and Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        working-directory: ./tailwindcss-semantic-colors

      - name: Run typecheck
        run: npm run typecheck
        working-directory: ./tailwindcss-semantic-colors

      - name: Run lint
        run: npm run lint
        working-directory: ./tailwindcss-semantic-colors

      - name: Run tests
        run: npm run test
        working-directory: ./tailwindcss-semantic-colors

      - name: Run build
        run: npm run build
        working-directory: ./tailwindcss-semantic-colors
